[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
testpaths = tests
addopts =
    --strict-markers
    --disable-warnings
    -ra
markers =
    postgres: requires PostgreSQL-specific features (exclusion constraints)
